---ds4.c---
@@ -10990,7 +10990,14 @@ static bool metal_graph_encode_token_raw_swa(
      * point where the prefix is large enough to hide useful work without
      * starving the second command buffer.
      */
+#ifdef __APPLE__
     uint32_t split_after_layers = 4;
+#else
+    /* CUDA command submission is not subject to the macOS display watchdog, and
+     * ds4_gpu_flush_commands() maps to a device synchronize there.  Avoid a
+     * per-token mid-graph sync unless explicitly requested for diagnostics. */
+    uint32_t split_after_layers = 0;
+#endif
     const char *split_env = getenv("DS4_METAL_GRAPH_TOKEN_SPLIT_LAYERS");
     if (split_env && split_env[0]) {
         char *end = NULL;
